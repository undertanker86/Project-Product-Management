form(form-search actionn="")
    .input-group
        input(
            type="text"
            placeholder="Enter keyword"
            name="keyword"
            class="form-control"
        )
        .input-group-append
            button(
            class="btn btn-success"
            type="submit"
            ) Search
